#include<stdio.h>
#include<conio.h>
#include<iostream>
class student
{
	public:
		int roll;
		char name[20];
		float per;
		void get()
		{
			std::cout<<"Enter roll,name &per\n";
			std::cin>>roll>>name>>per;
		}
		void disp()
		{
			std::cout<<"\n\Roll\tName\tPer\n";
			std::cout<<roll<<"\t"<<name<<"\t"<<per;
		}
};
int main()
{
	student s;
	s.get();
	s.disp();
	return 0;
}
